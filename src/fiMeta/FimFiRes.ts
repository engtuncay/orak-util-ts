// Codegen v2
import { FiMeta } from "../fiDtc/FiMeta";

export class FimFiRes {

  public static fsBoTknValid(): FiMeta {
    let fiMeta = FiMeta.create("fsBoTknValid");

    return fiMeta;
  }

  public static fsBoResult(): FiMeta {
    let fiMeta = FiMeta.create("fsBoResult");

    return fiMeta;
  }

  public static fsRefFdr(): FiMeta {
    let fiMeta = FiMeta.create("fsRefFdr");

    return fiMeta;
  }

  public static fsRefValue(): FiMeta {
    let fiMeta = FiMeta.create("fsRefValue");

    return fiMeta;
  }

  public static fsTxVer(): FiMeta {
    let fiMeta = FiMeta.create("fsTxVer");

    return fiMeta;
  }

  public static fsTxMessage(): FiMeta {
    let fiMeta = FiMeta.create("fsTxMessage");

    return fiMeta;
  }

  public static fsLnErrorCode(): FiMeta {
    let fiMeta = FiMeta.create("fsLnErrorCode");

    return fiMeta;
  }

  public static fsTxToken(): FiMeta {
    let fiMeta = FiMeta.create("fsTxToken");

    return fiMeta;
  }


}
