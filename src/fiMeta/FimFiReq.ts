// Codegen v2
import { FiMeta } from "../fiDtc/FiMeta";

export class FimFiReq {

  public static frTxProfile(): FiMeta {
    let fiMeta = FiMeta.create("frTxProfile");

    return fiMeta;
  }

  public static frTxUser(): FiMeta {
    let fiMeta = FiMeta.create("frTxUser");

    return fiMeta;
  }

  public static frTxPass(): FiMeta {
    let fiMeta = FiMeta.create("frTxPass");

    return fiMeta;
  }

  public static frTxToken(): FiMeta {
    let fiMeta = FiMeta.create("frTxToken");

    return fiMeta;
  }

  public static frFkbParams(): FiMeta {
    let fiMeta = FiMeta.create("frFkbParams");

    return fiMeta;
  }

  public static frBoShowDoc(): FiMeta {
    let fiMeta = FiMeta.create("frBoShowDoc");

    return fiMeta;
  }

  public static frTxDb(): FiMeta {
    let fiMeta = FiMeta.create("frTxDb");

    return fiMeta;
  }


}